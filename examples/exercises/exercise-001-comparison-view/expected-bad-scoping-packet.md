# Expected Bad Scoping Packet

This task is straightforward and should be implemented quickly.

We probably need to touch the frontend, backend, and tests.

Risks are low because comparison is a common feature.

We should add whatever components are needed and make sure it is accessible and secure.

GO.

## Why this is bad

- no file-level grounding
- no explicit boundaries
- "accessible and secure" used as slogans rather than review obligations
- assumes low risk without checking export and authorization surfaces
- no blocker handling for unclear API or schema
