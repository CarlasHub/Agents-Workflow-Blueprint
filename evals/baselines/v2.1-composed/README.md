# Frozen v2.1 composed prompt baselines

These 40 files were composed from `main` at `fb37a81b22e51d61d73e698c8a3514fbf01e3204` before the v3 prompt sources were rewritten. Each file contains the then-current copy-ready version of one prompt, including shared controls and governance.

They are immutable evaluation inputs for the `v2.1-prompt` condition. Do not regenerate them with the current composer or edit them to resemble v3. If a baseline is found to be corrupt, document the defect and recover the exact file from the recorded baseline commit so comparisons remain reproducible.

The deterministic runner requires one baseline filename per prompt ID and measures it with the same fictional case task used by the v3 conditions.
